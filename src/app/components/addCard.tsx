'use client';
import React from 'react'

const AddCard = () => {
  return (
    <div>
        <button onClick={() => console.log('add card')}>add card</button>
    </div>
  )
}

export default AddCard
